#def exporter_csv():
  
#        wait = WebDriverWait(navigateur, 30)
        
        # Attendre que le bouton soit cliquable
 #       bouton_export = wait.until(
 #           EC.element_to_be_clickable((By.ID, "btnExportCsv"))
 #       )
        
        # Faire défiler jusqu'au bouton
  #      navigateur.execute_script("arguments[0].scrollIntoView(true);", bouton_export)
 #       time.sleep(1)
        
        # Cliquer avec JavaScript au cas où
 #       navigateur.execute_script("arguments[0].click();", bouton_export)
        
  #      print("Click effectué, attente du téléchargement...")
        
        # Vérifier s'il y a une alerte
 #       try:
 #           WebDriverWait(navigateur, 5).until(EC.alert_is_present())
  #          alert = navigateur.switch_to.alert
  #          print(f"Alerte détectée: {alert.text}")
   #     except:
            print("Aucune alerte détectée")
        
        # Attendre que le bouton de statut apparaisse
        wait.until(EC.presence_of_element_located((By.ID, "btnStatus")))
        print("Téléchargement en cours...")
        
        # Attendre la fin du téléchargement (ajuster selon vos besoins)
        time.sleep(30)
        

# Appeler la fonction
exporter_csv()